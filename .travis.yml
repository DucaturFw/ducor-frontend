language: node_js
node_js:
  - "node"
cache:
  directories:
    - ~/.npm
    - ~/.cache
services:
  - docker
install:
  - yarn
script:
  - docker-compose -f docker-compose.test.yml run cypress
after_script:
  - docker-compose down